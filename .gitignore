# Python bytecode
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
.venv/
venv/
env/

# Environment config (may contain credentials in future stages)
.env

# Distribution / packaging
*.egg-info/
dist/
build/

# OS noise
.DS_Store
Thumbs.db

# LLM model weights — too large for version control
models/*.gguf
models/*.bin
models/*.ggml

# Meeting records — personal data, not for version control
meetings/*.json
